apply plugin: HextantLibrary
apply plugin: 'maven-publish'

jar {
    manifest {
        attributes('Plugin-Initializer' : 'Core')
    }
}

dependencies {
    compile nikok("reaktive", "0.1.1-SNAPSHOT")
    compile nikok("kserial", "0.1-SNAPSHOT")
    compile kotlin("stdlib")
    compile kotlin("reflect")
    compile nikok("kref", "0.1-SNAPSHOT")
    compile "org.controlsfx:controlsfx:8.40.14"
    implementation project(":hextant-util")
    testImplementation project(":hextant-expr")
    testImplementation project(":hextant-test")
}

task sourcesJar_(type: Jar) {
    classifier = 'sources'
    from sourceSets.main.allSource
}

task javadocJar_(type: Jar) {
    classifier = 'javadoc'
    from javadoc.destinationDir
}

publishing {
    publications {
        maven(MavenPublication) {
            from components.java
            artifact sourcesJar_
            artifact javadocJar_
        }
    }
}
version '1.0-SNAPSHOT'
group 'org.hextant'

sourceCompatibility = 1.8

dependencies {
    compileOnly project(":hextant-core")
    compile(spek("api")) {
        exclude group: "org.jetbrains.kotlin"
    }
    runtime(spek("junit-platform-engine")) {
        exclude group: "org.jetbrains.kotlin"
    }
    compile "com.natpryce:hamkrest:1.5.0.0"
}